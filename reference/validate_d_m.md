# Validate d_m string

Ensure d_m is a supported pair of design and model. If d_m is just a
design, select a default model. Convert PowerUp! names to our naming
system as needed.

## Usage

``` r
validate_d_m(d_m)
```

## Value

Full d_m string that will be found in \`pump_info()\`
